syntax = "v1"

import "types.api"

@server(
    group: opensql/v1
    prefix: opensql/v1
    jwt: JwtAuth
)

service opensql {
    @handler exec
    post /exec (ExecReq) returns (ExecResp)

    @handler raw
    post /raw (RawReq) returns (RawResp)
}
